server:
  servlet:
    context-path: /api
  port: 8090
spring:
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    generate-ddl: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: true
    hibernate:

      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      use-new-id-generator-mappings: false
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://j7d106.p.ssafy.io:3306/doseok?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: hive
    password: nh+7&(tUa7p[NYcAae{Sno&LM698=1
#  h2:
#    console:
#      enabled: true
#  datasource:
#    url: jdbc:h2:mem:test db;MODE=MySQL;DATABASE_TO_UPPER=FALSE;

logging:
  level:
    org.hibernate.sql: debug

